{
  "name": "typescript-lessons",
  "version": "1.0.0",
  "description": "Sample typescript app",
  "main": "app.js",
  "scripts": {
    "compile:type": "tsc --watch",
    "server:live": "live-server --port=8090 --quiet",
    "serve": "npm-run-all --parallel compile:type server:live"
  },
  "author": "Peter Kitonga",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5"
  }
}
